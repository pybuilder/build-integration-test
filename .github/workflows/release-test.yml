name: Release Test
on:
  push:
    branches: ['integration-test/**']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      # This workflow is overwritten per-run by trigger.yml with the correct action SHA.
      # The version on master is a placeholder to enable the push trigger.
      - run: echo "This placeholder should never run"
